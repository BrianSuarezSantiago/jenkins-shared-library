@Library('shared-library') _

pipeline {
    agent any
    stages {
        stage('Prueba') {
            steps {
                ejemplo('carlos')
                // ejemplo.call()
            }
        }
    }
}

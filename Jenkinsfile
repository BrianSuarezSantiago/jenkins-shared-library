@Library('shared-library') _

pipeline {
    agent any
    stages {
        stage('Prueba') {
            steps {
                ejemplo('pepe')
                // ejemplo.call()
            }
        }
    }
}
